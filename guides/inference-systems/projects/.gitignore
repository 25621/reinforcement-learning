# Run logs are regenerated by `python3 run.py`; the committed results live in
# each project's outputs/findings.json, outputs/*.csv and outputs/*.png.
**/outputs/run.log
**/outputs/serve.log
**/__pycache__/
# Phase 7 replica server logs (one per fleet process; regenerated each run)
**/outputs/r[0-9].log
